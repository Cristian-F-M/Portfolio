---
import { twMerge } from "tailwind-merge";

interface Props {
	class?: string;
}

const { class: className } = Astro.props;
---

<span class={twMerge("flex flex-row gap-2 items-center bg-secondary/20 w-fit px-3 py-2.5 rounded-lg border border-tertiary/50", className)}>
	<slot />
</span>
